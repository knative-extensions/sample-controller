run:
  timeout: 5m

  build-tags:
    - e2e

  skip-dirs:
    - pkg/client

linters:
  enable:
    - asciicheck
    - errorlint
    - golint
    - gosec
    - prealloc
    - stylecheck
    - tparallel
    - unconvert
    - unparam
  disable:
    - errcheck
